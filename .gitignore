node_modules/

# What `npm run build` produces: `public/` and lily's admin UI, merged.
dist/

# wrangler's workspace and generated types.
.wrangler/
worker-configuration.d.ts

# **Your local secrets.** Copy `.dev.vars.example` and fill it in.
.dev.vars
